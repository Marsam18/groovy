import React from 'react';

function CartPage() {
  return (
    <div className="cart-page">
      <h1>Carrito de compras</h1>
      {/* Agrega aquí el contenido de tu carrito, como los productos, precios, cantidades, etc. */}
    </div>
  );
}

export default CartPage;
